import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        common: {
            white: '#ffffff',
            grayscale: '#fafbfb',
            gradientnavy: '#16113a',
            grayblack: '#212121',
        },
        // Define other palette properties if necessary
    },
    // Other theme settings can go here
});


export default theme;
